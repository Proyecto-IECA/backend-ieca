class VistaVacante {
    constructor(
        id_vista_vacante,
        id_vacante,
        id_postulante,
        fecha
    ) {
        (this.id_vista_vacante = id_vista_vacante),
        (this.id_vacante = id_vacante),
        (this.id_postulante = id_postulante),
        (this.fecha = fecha);
    }
}